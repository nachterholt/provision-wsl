[core]
  pager = delta

[delta]
  navigate = true
  line-numbers = true

[interactive]
  diffFilter = delta --color-only

# Persönliche Daten aus lokaler Datei laden
[include]
  path = ~/.gitconfig.local